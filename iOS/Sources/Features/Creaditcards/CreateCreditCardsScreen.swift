import SwiftUI

struct CreateCreditCardsScreen: View {
    var body: some View {
        Form {
            Text("Formulário de criação de cartão")
        }
        .navigationTitle("Novo Cartão")
    }
}

#Preview {
    CreateCreditCardsScreen()
}
