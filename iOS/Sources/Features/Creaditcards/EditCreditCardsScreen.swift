import SwiftUI

struct EditCreditCardsScreen: View {
    var body: some View {
        Form {
            Text("Edição de cartão")
        }
        .navigationTitle("Editar Cartão")
    }
}

#Preview {
    EditCreditCardsScreen()
}
